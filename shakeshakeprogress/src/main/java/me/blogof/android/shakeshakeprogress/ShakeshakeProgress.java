package me.blogof.android.shakeshakeprogress;

import android.content.Context;
import android.graphics.Canvas;
import android.util.AttributeSet;
import android.view.View;

/**
 * Created by dzq on 16/1/18.
 */
public class ShakeshakeProgress extends View {

    public ShakeshakeProgress(Context context) {
        super(context);
    }

    public ShakeshakeProgress(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    @Override
    protected void onDraw(Canvas canvas) {
        super.onDraw(canvas);

    }
}
